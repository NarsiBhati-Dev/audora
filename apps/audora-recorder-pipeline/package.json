{
  "name": "audora-recorder-pipeline",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "kafka": "docker-compose up -d",
    "consumer": "bun run src/consumers/consumer.ts",
    "dev": "bun run kafka & sleep 5 && bun run consumer",
    "stop": "docker-compose down"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.839.0",
    "@types/kafkajs": "^1.9.0",
    "kafkajs": "^2.2.4",
    "mime-types": "^3.0.1"
  }
}
