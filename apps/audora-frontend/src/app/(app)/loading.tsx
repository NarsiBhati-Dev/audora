// This file is intentionally empty
// We now use HeaderLoader and skeleton cards instead of full-screen loading
// The full-screen loading is only used for auth/initial page loads

export default function Loading() {
  return null;
}
